package indigoplugin.core

class IndigoBuildTests extends munit.FunSuite {

  test("Asset filters (default)") {
    //isCopyAllowed
    assert(1 == 2)
  }

  test("Asset filters (exclude)") {
    assert(1 == 2)
  }

  test("Asset filters (include)") {
    assert(1 == 2)
  }

  test("Asset filters (include inside excluded folder)") {
    assert(1 == 2)
  }

}
